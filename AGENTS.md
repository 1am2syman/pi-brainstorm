# Maintenance

- Preserve both distribution files: `prompts/brainstorm.md` provides the exact `/brainstorm` TUI command, while `skills/brainstorm/SKILL.md` owns mode behavior.
- Keep `/approved` unregistered; it is recognized only as an inline token while brainstorm mode is active.
