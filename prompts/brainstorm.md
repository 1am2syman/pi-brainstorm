---
description: Toggle persistent discussion-only brainstorm mode
argument-hint: "[query]"
---
Invoke the globally installed `brainstorm` skill now. Treat this invocation as its mode toggle and follow it exactly.

Query supplied with this invocation:
${ARGUMENTS:-No query was supplied.}
